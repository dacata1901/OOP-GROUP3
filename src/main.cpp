#include "Game.h"
#include <iostream>

using namespace std;

int main() {
    // Tạo đối tượng Game (bộ điều khiển chính)
    Game game;

    // Gọi hàm run để bắt đầu game
    game.run();

    return 0;
}